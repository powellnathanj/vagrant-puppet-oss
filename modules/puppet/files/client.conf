[main]
    logdir = /var/log/puppet
    rundir = /var/run/puppet
    ssldir = $vardir/ssl
    server = puppet.nathanpowell.test

[agent]
    classfile = $vardir/classes.txt
    localconfig = $vardir/localconfig
